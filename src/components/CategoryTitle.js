
const CategoryTitle = (props) => {
    return (
        <div className="category__title">
            <h1>
                {props.title}
            </h1>

        </div>
    );
}

export default CategoryTitle;